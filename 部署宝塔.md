# 宝塔部署说明

## 1. 初始化数据库

在宝塔的 phpMyAdmin 或 Navicat 中导入项目根目录的 `init.sql`。脚本会创建 `ops_box` 数据库和 `user`、`email_code`、`announcement`、`app_version` 四张表，兼容 MySQL 5.7。

在宝塔数据库管理中创建一个对 `ops_box` 有读写权限的数据库账号，将账号和密码填入 `.env`。

## 2. 配置环境变量

将 `.env.example` 复制为 `.env`，保留已有 `OPSHUB_AI_*` 配置，并填写 MySQL、JWT 和 QQ 邮箱 SMTP 项。也可以在宝塔 Node 项目管理器的项目环境变量中设置同名配置。

- `PORT=3000`
- `MYSQL_HOST=127.0.0.1`，宝塔本机 MySQL 不需要开放公网端口
- `JWT_SECRET` 使用长度至少 32 位的随机字符串
- QQ 邮箱使用授权码填写 `EMAIL_PASS`，SMTP 固定使用 SSL 465 端口

## 3. 配置 Node 项目

在宝塔 Node 项目管理器中：

1. Node 版本选择 18 或更高版本。
2. 项目目录选择本项目根目录。
3. 启动文件设置为 `server.js`，项目端口设置为 `3000`。
4. 安装依赖：`npm install --omit=dev`。
5. 启动项目并查看运行日志。

服务会监听 `0.0.0.0:3000`。在宝塔防火墙和阿里云安全组中同时放行 TCP `3000`；服务器公网地址为 `http://8.134.94.184:3000`。如通过宝塔 Nginx 反向代理对外提供 HTTPS，代理目标填写 `http://127.0.0.1:3000`，此时只需对外放行 80/443。

## 4. 创建首个管理员

注册接口只能创建普通用户。完成邮箱注册后，在 phpMyAdmin 或 Navicat 中执行以下参数化等价的管理操作，将首个账号提升为管理员：

```sql
UPDATE `user` SET `role` = 'admin' WHERE `email` = 'admin@example.com';
```

将示例邮箱替换为实际管理员邮箱。之后该管理员登录取得的 JWT 可发布公告；普通用户只能读取公告和使用受保护的 AI 接口。

## 5. 更新包静态站点

在宝塔新建一个纯静态网站，将 Tauri 打包生成的 exe 更新包上传到该站点目录。静态站点只需在宝塔和阿里云安全组中放行 TCP `80`；如配置 HTTPS，同时放行 `443`。

管理员调用 `POST /api/version/publish` 时，将该更新包的公网 URL 填入 `download_url`，例如 `http://download.example.com/OpsBox-1.0.2.exe`。客户端启动时会匿名静默读取 `GET /api/version/latest`，仅当服务端版本高于程序版本时显示更新弹窗。
